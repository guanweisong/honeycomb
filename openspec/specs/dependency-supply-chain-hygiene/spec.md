## Purpose

确保生产依赖不包含仅用于开发的工具，并及时修复直接依赖中的已知安全公告。

## Requirements

### Requirement: 直接依赖漏洞修复和开发工具隔离

系统 MUST 使用不受已知公告影响的直接运行时依赖版本，并将只在开发中调用的组件生成 CLI 保留在开发依赖中。

#### Scenario: 安装生产依赖

- **WHEN** 生产部署安装依赖
- **THEN** 不安装 `shadcn` CLI，且富文本清洗、数据库、国际化和 CSS 直接依赖使用已修复版本
