## 1. 评论成功语义

- [ ] 1.1 先添加失败测试，证明通知查询或邮件发送失败时已创建评论仍应返回成功
- [ ] 1.2 调整评论创建用例，以数据库创建结果构造返回值，并把完整通知流程放入 best-effort 错误边界
- [ ] 1.3 添加脱敏结构化日志断言，并验证数据库创建失败时不调用通知

## 2. 媒体删除编排

- [ ] 2.1 先添加失败测试，固定 R2 失败保留数据库记录、R2 成功后才删除记录和重复删除幂等语义
- [ ] 2.2 在 Media Application 定义最小待删除记录契约和 `MediaStorage` 删除端口
- [ ] 2.3 将 Media Repository 拆为读取删除目标与删除数据库记录，不再导入 S3
- [ ] 2.4 实现存储优先的 `destroyMedia` Use Case，并更新 Router 依赖装配
- [ ] 2.5 更新 Repository、S3 adapter 和 tRPC 回归测试

## 3. 验证

- [ ] 3.1 运行 Comment、Media、S3 和相关 Router 聚焦测试
- [ ] 3.2 运行类型检查、Lint、完整单元测试、覆盖率、生产构建和 `git diff --check`
- [ ] 3.3 运行可用的评论与媒体 E2E，记录因外部环境无法执行的项目
