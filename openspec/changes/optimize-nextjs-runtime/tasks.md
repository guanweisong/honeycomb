## 1. 缓存失效闭环

- [x] 1.1 先为菜单、设置、分类、标签和公开作者资料写入补充失败的 Router 缓存失效测试
- [x] 1.2 在成功持久化后接入统一公开缓存失效器，并验证失败写入不会失效缓存
- [x] 1.3 增加写操作到公开渲染影响面的静态治理测试，防止新增写入口漏接失效

## 2. 服务端请求级查询复用

- [x] 2.1 先增加文章、页面、设置、菜单和评论等价读取只执行一次的失败测试
- [x] 2.2 建立 `server-only` 的 React `cache()` 公开 Query 入口并迁移页面、布局和 Metadata
- [x] 2.3 让详情页评论计数与评论组件共享同一读取结果，删除重复 server caller 路径

## 3. 响应式图片

- [x] 3.1 先增加图片尺寸目录、输出格式和关键 `sizes` 属性的失败测试
- [x] 3.2 调整 `deviceSizes`、`imageSizes`、AVIF/WebP 格式及小图和媒体网格的 `sizes`
- [x] 3.3 运行图片相关组件测试与配置测试，确认静态图片和远程图片行为兼容

## 4. 后台客户端依赖下沉

- [x] 4.1 先增加普通后台路由不依赖编辑器媒体选择实现的失败边界测试
- [x] 4.2 从 `AdminProviders` 移除媒体选择器，将局部 Provider 下沉到文章和页面编辑路由
- [x] 4.3 对 Tiptap、图表、媒体选择器和可排序树建立路由级或 `next/dynamic` 延迟加载与可访问 fallback
- [ ] 4.4 运行后台组件、编辑器和边界测试，并通过生产 manifest 对比公共依赖

## 5. PWA 预缓存预算

- [x] 5.1 先增加预缓存排除模式和离线必要资源保留的失败测试
- [x] 5.2 配置 Serwist 排除 Manifest screenshots、source map 和非必要后台资产
- [ ] 5.3 运行 PWA 单测与生产构建，记录资源数量和预缓存体积

## 6. 动态浏览量

- [x] 6.1 先增加 mutation 返回计数、客户端更新显示、失败隔离和不失效 SSG 的失败测试
- [x] 6.2 复用已发布内容 increment mutation 的返回计数更新显示，不追加重复查询
- [x] 6.3 从静态 PostInfo 数据流移除最终浏览量权威值，保持无 JavaScript 兜底语义

## 7. 有界 API 限流

- [x] 7.1 先增加 Proxy 限流超时、生产失败关闭、开发本地允许和指标结果的失败测试
- [x] 7.2 为全局限流实现可注入有界等待与显式环境降级策略
- [x] 7.3 先增加高成本 procedure 独立限额在 handler 前拒绝的失败测试
- [x] 7.4 建立可组合 tRPC 限流 procedure/middleware，并为选定高成本入口接线

## 8. Metadata 与可访问性

- [x] 8.1 先增加 `metadataBase`、canonical、hreflang、默认社交图片、not-found 和 viewport 的失败测试
- [x] 8.2 在公开根布局与动态页面补全 Metadata 默认值、本地化 canonical 和 alternates
- [x] 8.3 新增可访问的全局 not-found 页面，并移除后台禁止缩放的 viewport 配置

## 9. 综合验证与收尾

- [ ] 9.1 运行相关单测、类型检查、Lint、架构边界测试和 `git diff --check`
- [ ] 9.2 运行全量单测与覆盖率，确认门禁未下降
- [ ] 9.3 运行关键公开站点、后台编辑、RBAC、PWA、无障碍和性能 E2E
- [ ] 9.4 运行 Next.js 生产构建与 bundle analysis，记录路由分类、警告、后台公共 JS 和 PWA 体积
- [ ] 9.5 更新 OpenSpec 任务状态和验证记录，确认没有旧入口、重复定义或未验证边界
