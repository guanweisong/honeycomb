## 1. 路由处理

- [x] 1.1 检查现有博客文章路由，定位当前是在哪里解析缺失文章的。
- [x] 1.2 按现有博客 404 行为的相同模式，为无法解析的文章 slug 增加 not-found 处理。
- [x] 1.3 保持有效文章 slug 的正常渲染不变。

## 2. 验证

- [x] 2.1 新增或更新测试，覆盖无效、已删除和已存在的文章 slug。
- [x] 2.2 验证文章路由在缺失文章时返回 not-found，并且在有效文章时仍可正常渲染。
