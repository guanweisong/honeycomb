# 设计

## 概述

博客文章页的解析应把发布状态视为路由有效性的一部分。页面在“缺失”和“未发布”这两种情况下不应有任何区别，最终都必须返回 404。

## 方案

- 复用现有的博客文章查询路径解析文章记录。
- 在渲染页面之前，同时检查记录是否存在以及是否已发布。
- 如果查询失败，或者文章处于未发布状态，就返回框架提供的 not-found 响应。
- 对已发布文章保持原有成功路径不变。

## 注意事项

- 该检查必须发生在任何可能暴露过期数据的页面渲染之前。
- 这应与现有博客路由保持一致，不引入单独的兜底页。
- 测试需要覆盖 3 种失败场景和 1 种成功场景。
