## 1. 工程规范与变更基线

- [x] 1.1 将行为保持不变和重构范围护栏写入 `frontend-structure` 技能
- [x] 1.2 补充 OpenSpec 变更校验并确认任务与代码范围一致

## 2. Admin 身份边界

- [x] 2.1 为 Application/Identity 的 Admin 用户查询窄接口编写失败测试
- [x] 2.2 实现查询用例并让 `getAdminUser` 通过该用例工作
- [x] 2.3 增加 App Router 不直接导入数据库基础设施的边界测试
- [x] 2.4 运行 Admin 身份相关测试、包边界测试和类型检查

## 3. 授权矩阵拆分

- [x] 3.1 为矩阵数据与 AST 扫描职责分离编写结构测试
- [x] 3.2 将静态矩阵数据与扫描规则拆分为职责单一模块
- [x] 3.3 保持原有授权矩阵测试和错误语义不变

## 4. 大型业务模块拆分

- [x] 4.1 拆分评估中识别的权限静态配置/复杂页面职责，并保持原入口导出
- [x] 4.2 为拆分出的纯函数、转换和视图边界补充相邻测试
- [x] 4.3 检查客户端边界、依赖方向和文件规模，删除无意义包装

## 5. 验证与交付

- [x] 5.1 运行聚焦单元测试和授权矩阵测试
- [x] 5.2 运行 `bun run check-types`、`bun run lint`、覆盖率测试和构建
- [x] 5.3 运行 `openspec validate harden-admin-boundaries-and-split-modules` 并检查 diff
