## 1. 用户响应与权限

- [x] 1.1 为用户创建、更新安全 DTO 和访客访问用户列表编写失败测试
- [x] 1.2 将用户创建、更新改为显式字段投影，并从用户列表权限中移除 `GUEST`
- [x] 1.3 运行用户 Router 测试确认安全响应与权限规则通过

## 2. 评论安全边界

- [x] 2.1 为公共创建响应、目标可见性、评论开关、单一目标和父子同源规则编写失败测试
- [x] 2.2 实现公共评论 DTO 和共享评论目标解析/可见性校验
- [x] 2.3 在 `comment.listByRef` 和 `comment.create` 接入目标校验
- [x] 2.4 在评论创建前验证单一目标及父评论资源元组
- [x] 2.5 保持邮件通知使用私有服务端数据，但只向客户端返回公共 DTO
- [x] 2.6 运行评论 Router 测试确认隐私字段和无效关系均被阻断

## 3. 已发布内容辅助接口

- [x] 3.1 为文章分类查询及文章、页面浏览量更新的发布状态约束编写失败测试
- [x] 3.2 在三个接口的 SQL 谓词中强制 `PUBLISHED` 并处理未命中情况
- [x] 3.3 运行文章和页面 Router 测试确认未发布内容不可查询或更新

## 4. 全面验证

- [x] 4.1 搜索后台与前台调用点，确认公开和后台入口使用正确
- [x] 4.2 运行 TypeScript 类型检查和 ESLint
- [x] 4.3 运行完整单元测试套件
- [x] 4.4 运行 Next.js 生产构建并检查 Git 差异