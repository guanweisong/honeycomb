## 动机

现有 strict 检查没有覆盖索引空值，持久化读取、表单提交和外部 JSON 仍存在不真实的类型承诺。此次根据工程审查结果修正跨层契约，并建立持续检查。

## 变更内容

- 开启索引空值检查，修正生产代码、脚本和测试夹具的缺失值处理。
- 持久化边界验证领域枚举，明确写入未返回记录与查询缺失的语义。
- 收紧查询、表单和外部数据的契约，消除可建模的业务断言。
- 补充回归测试和类型治理门禁，记录必要的第三方适配边界。

## 能力

### 新增能力

无。

### 修改能力

- `type-safety-governance`：增加索引安全与运行时边界验证要求。

## 影响

涉及 tsconfig、业务 Repository/DTO、前端表单、共享类型工具和测试。保持已有有效输入、成功响应结构、数据库 schema、迁移和依赖不变；非法持久化值和不存在的写入目标改为明确失败。
