## 1. 基线与测试

- [x] 1.1 为环境密钥强度、生产限流故障和 `proxy` 响应编写先失败的单元测试。
- [x] 1.2 为公共站点、后台和全局路由恢复边界编写组件测试。
- [x] 1.3 补齐环境解析分支测试，使全部已声明关键覆盖率门禁可通过。

## 2. 运行时安全与框架迁移

- [x] 2.1 在生产环境变量解析中实现 `AUTH_SECRET` 的长度和熵校验。
- [x] 2.2 将 Next.js 请求入口从 `middleware.ts` 迁移为 `proxy.ts` 并保持 matcher 与 i18n 行为。
- [x] 2.3 实现显式的生产限流可用性策略、503 降级响应及非生产内存策略。
- [x] 2.4 实现公共站点、后台和根布局的 loading/error 恢复边界。

## 3. 依赖与持续交付

- [x] 3.1 升级直接和传递依赖，移除仅开发使用工具的生产依赖，并复核锁文件审计。
- [x] 3.2 创建 GitHub Actions 工作流，执行冻结安装、类型、lint、覆盖率、构建、审计和关键 E2E。
- [x] 3.3 更新运行与部署文档，说明生产 Upstash 和认证密钥要求。

## 4. 验证与收尾

- [ ] 4.1 执行类型检查、lint、覆盖率和生产构建验证。
  - 类型检查、lint、覆盖率已通过；Turbopack 生产构建在本地及沙箱外均因处理 `photoswipe.css` 时无法创建进程/绑定端口而阻塞，待 CI 或真实部署环境复核。
- [x] 4.2 执行关键 Playwright E2E 并复核 Next.js 弃用警告已消失。
- [x] 4.3 复核 `bun audit` 结果，记录任何不可立即消除的生产可达例外。
