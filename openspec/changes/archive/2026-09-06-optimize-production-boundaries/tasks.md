## 1. 公开内容输出安全

- [x] 1.1 为 JSON-LD 脚本闭合串编写失败测试并确认当前序列化输出不安全。
- [x] 1.2 实现唯一安全 JSON 序列化器并接入文章详情页。
- [x] 1.3 为危险富文本真实渲染编写失败测试并确认当前输出包含危险属性或 URL。
- [x] 1.4 在 RichText 展示边界接入现有清洗器并运行聚焦测试。

## 2. 浏览量与缓存边界

- [x] 2.1 更新文章与页面渲染测试，确认当前 Server Component 会执行浏览量写入。
- [x] 2.2 删除 Server Component 写副作用，新增一次挂载一次、失败隔离的客户端浏览量上报组件及测试。
- [x] 2.3 为缓存刷新结构化输入编写失败测试，覆盖任意路径、非法内容类型和非法内容标识。
- [x] 2.4 将评论刷新改为服务端构造受控公开路径，并更新评论提交 Hook 测试。
- [x] 2.5 审计文章、页面和评论写入口，为成功写入接入对应公开路径缓存失效并补充行为测试。

## 3. CI 与端到端门禁

- [x] 3.1 将 bundle 分析切换为 `--output` 批处理模式，并让 CI 保存静态诊断结果。
- [x] 3.2 识别无需外部数据库即可稳定运行的现有 E2E，并纳入 CI 或记录真实阻塞边界。

## 4. 文档、规格与依赖

- [x] 4.1 修正 README 中 Turnstile 条件启用说明。
- [x] 4.2 补全八个既有主规格的 Purpose，使全量 OpenSpec 严格校验通过。
- [x] 4.3 对齐 Next.js 与 ESLint 配置版本，移除确认未使用的 bcryptjs 类型占位和复制工具依赖，并更新锁文件。

## 5. 验证

- [x] 5.1 运行聚焦测试、类型检查、Lint、架构边界测试和 `git diff --check`。
- [x] 5.2 运行完整单元测试、生产构建、可执行 E2E、依赖审计和 OpenSpec 严格校验。
- [x] 5.3 更新 verification，复扫危险输出、Server Component 写入、任意缓存路径和未使用依赖，确认任务与结果一致。
