## 1. 后台账号凭据策略

- [x] 1.1 先添加用户写入 Schema 与最终凭据写入的失败测试，覆盖非法邮箱、短密码和超长密码
- [x] 1.2 在身份包定义唯一密码策略，并让 Better Auth、用户应用层 Schema 和凭据写入共同复用
- [x] 1.3 运行相关单元测试与类型检查，确认创建和更新路径均受保护

## 2. 生产依赖审计门禁

- [x] 2.1 先添加带 ANSI/Bun 前缀、无 JSON 和多 JSON 的解析失败测试
- [x] 2.2 实现结构感知且失败关闭的审计 JSON 解析器并接入真实命令路径
- [x] 2.3 运行审计单元测试与真实 `audit:production` 门禁

## 3. 完整 E2E 门禁

- [x] 3.1 先扩展质量工作流契约测试，要求播种步骤、完整 Chromium 命令及零条件跳过
- [x] 3.2 实现带本地 URL 和显式开关保护的幂等 E2E 种子脚本及其安全测试
- [x] 3.3 更新质量工作流和 package scripts，在迁移后播种并运行完整 Chromium 项目
- [x] 3.4 将主题和标签 E2E 的条件跳过替换为确定性失败断言

## 4. 集成验证

- [x] 4.1 在临时本地数据库执行迁移和播种，并运行完整 Chromium E2E
- [x] 4.2 运行单元测试、lint、类型检查、生产依赖审计和构建门禁
- [x] 4.3 检查最终 diff、同步 OpenSpec 主规格并确认任务全部完成
