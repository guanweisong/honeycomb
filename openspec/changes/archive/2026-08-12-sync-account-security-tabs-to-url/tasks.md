## 1. 实现

- [x] 1.1 将账号安全标签定义抽离为可校验的稳定配置。
- [x] 1.2 新建客户端标签导航容器，同步 `tab` 查询参数与受控标签状态。
- [x] 1.3 在服务端页面增加 Suspense 边界，并向客户端容器传递社交登录提供方。

## 2. 验证

- [x] 2.1 为 URL 初始值、无效值回退和标签切换 URL 更新补充测试。
- [x] 2.2 运行账号安全组件测试、类型检查与相关静态检查。
