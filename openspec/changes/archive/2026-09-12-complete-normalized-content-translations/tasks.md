## 1. 评论关联翻译

- [x] 1.1 先增加评论后台关联文章、页面和自定义文章标题的翻译表回归测试，并确认旧实现失败
- [x] 1.2 批量加载并组装评论关联标题，保持查询次数与评论数量无关

## 2. 必填翻译恢复

- [x] 2.1 先增加分类、页面和标签缺失 locale 行后更新可恢复的仓储测试，并确认旧实现失败
- [x] 2.2 将分类、页面和标签的完整翻译更新改为事务内按复合主键 upsert

## 3. 契约与文档清理

- [x] 3.1 将生产代码中重复手写的多语言文本类型替换为权威 `MultiLang` 派生类型
- [x] 3.2 删除 schema 测试中的旧自定义字段序列化器类型残留
- [x] 3.3 更新 README、多语言类型注释和归档设计中的过时描述

## 4. 验证

- [x] 4.1 运行针对性单测、类型检查、Lint 和完整单元测试，不运行 E2E
- [x] 4.2 运行 OpenSpec 严格校验和 `git diff --check`，审查最终差异及工作区边界
