# 工程复杂度治理

## 要求

### 要求：Router 仅承担传输编排

复杂业务 Router MUST 将数据库写操作、事务、缓存失效和业务判断委托给同一业务模块的 Service/Policy/Repository，并保持公开 procedure 行为不变。

### 要求：大型模块按职责拆分

权限矩阵、数据库 Schema 和大型测试 MUST 将静态数据、执行逻辑和行为断言分离；拆分后 MUST 保留已有稳定导出。

### 要求：删除必须有证据

依赖或公共抽象只有在源码、配置、动态导入和构建脚本均确认无调用方后才 MAY 删除。
