## 1. 持久化契约

- [x] 1.1 为缺失写入记录及非法枚举补充失败测试，修正所有受影响的 Repository 与领域 DTO。
- [x] 1.2 收紧查询参数和读取映射，消除数据库模型整体断言。

## 2. 前端与外部边界

- [x] 2.1 用真实 schema 产物提交表单，修正路由空参数、表格读取和可空展示数据。
- [x] 2.2 验证外部 JSON 与缓存数据，修正共享层可消除的类型断言。

## 3. 编译与持续治理

- [x] 3.1 修正脚本和测试夹具的索引访问，启用 noUncheckedIndexedAccess。
- [x] 3.2 补充类型治理回归测试并修复影响验证的 CI 环境断言。
- [x] 3.3 运行类型检查、Lint、全量测试、覆盖率、进程门禁和隔离生产构建；记录结果和剩余适配边界。

## 4. 唯一事实源（复审后追加）

- [x] 4.1 先增加唯一出口、共享 schema 和缓存往返门禁，确认当前重复定义使测试失败。
- [x] 4.2 统一多语言规则及媒体/标签/文章读取 schema，类型从权威 schema 推导；跨功能仅通过公开 Application 边界复用。
- [x] 4.3 将 contracts 改为唯一模型的重新导出，区分公开/后台评论；复用当前用户类型。
- [x] 4.4 统一写入结果检查、缓存常量、表格元数据和标签字段配置，删除无消费者的重复类型。
- [x] 4.5 运行类型、Lint、全量覆盖率、流程与隔离构建检查，复扫重复定义并记录保留的不同语义。
- [x] 4.6 将生成前的唯一事实源规则写入类型治理技能，由前端与 DDD 技能引用，并进行独立场景验证。

## 5. 全面复审遗漏收敛

- [x] 5.1 为写入契约所有权及配置复用增加失败门禁；将 Post/Page/Media/Link 写入 schema 移到所属 Application，类型推导，transport 重新导出。
- [x] 5.2 统一评论映射、文章列表/详情结果及 AdminUser 类型，保留公开与后台语义。
- [x] 5.3 统一 OAuth 提供商筛选/名称及登录历史事件契约，迁移生产消费者。
- [x] 5.4 统一分页基础类型与具名默认值、领域枚举引用、语言列表/清洗和缓存命名空间目录；删除无消费者的可见性枚举。
- [x] 5.5 验证类型、Lint、全量覆盖率、进程测试、隔离构建及新增门禁，并复扫和记录边界。
