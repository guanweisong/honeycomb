## 1. 持久化模型与迁移保护

- [x] 1.1 为五张实体翻译表编写失败的 schema、关系、语言约束和级联删除测试
- [x] 1.2 在 Drizzle schema 与 relation 定义中新增五张翻译表，并从五张主表删除 15 个 JSON 多语言列
- [x] 1.3 为旧 JSON 有效性、分类/页面/标签完整双语和迁移后数据一致性编写失败的迁移测试
- [x] 1.4 生成并审查版本化迁移，加入失败保护、无损回填、主表重建、索引/外键恢复和迁移后完整性校验
- [x] 1.5 在独占临时 `file:` 数据库上验证 baseline 到目标 schema 的迁移，并确认数据库、WAL/SHM 和临时目录均已清理

## 2. 翻译映射与写入事务

- [x] 2.1 为翻译行拆分、批量组装、缺失语言和空行删除语义编写失败的纯映射测试
- [x] 2.2 在各 Feature Infrastructure 内实现复用权威 locale 目录的类型安全翻译映射，不新增通用多态 Repository
- [x] 2.3 为分类、页面和标签的原子创建、完整更新与回滚编写失败的 Repository 测试
- [x] 2.4 将分类、页面和标签写入迁移为主表与翻译表同事务操作，并保持 Application DTO 不变
- [x] 2.5 为文章可空翻译与网站设置按字段、按语言局部更新编写失败的 Repository 测试
- [x] 2.6 将文章和网站设置写入迁移为按 locale upsert/清理的事务操作，保留未提供值并处理显式 `null`

## 3. 读取、搜索与关系组装

- [x] 3.1 为五类实体的稳定 `{ zh, en }` 读取模型和批量翻译加载编写失败测试
- [x] 3.2 改造五类 Repository 查询与 mapper，从翻译行组装现有读取模型并禁止 ORM 翻译行泄漏
- [x] 3.3 为跨语言搜索、无重复分页和准确总数编写失败测试
- [x] 3.4 使用相关 `EXISTS` 翻译子查询替换 JSON 文本搜索，保持主表排序与分页行为
- [x] 3.5 更新菜单、评论通知、分类/标签关系、富文本媒体提取和缓存读取路径，确保关联翻译批量组装且无 N+1

## 4. 契约清理与本地质量门禁

- [x] 4.1 更新受影响的 fake、fixture、schema 测试、关系完整性测试、类型治理和 Feature 边界测试
- [x] 4.2 删除 `i18n-field.ts`、旧 JSON mapper、旧查询分支和全部遗留引用，确认多语言契约仍只有一个权威来源
- [x] 4.3 运行类型检查、Lint、完整单元测试、迁移治理、OpenSpec 严格校验、生产构建和 `git diff --check`，明确跳过 E2E

## 5. 远程备份、数据迁移与同步

- [x] 5.1 只读解析并脱敏确认目标 Turso 数据库，审计远程 schema、migration ledger、JSON 有效性和必填翻译数据
- [x] 5.2 在仓库外私有目录导出远程 snapshot/WAL，设置安全权限，并通过 `PRAGMA integrity_check`、关键表计数与 SHA-256 验证备份
- [x] 5.3 在已批准维护窗口仅使用 `bun run db:migrate` 应用已提交迁移，不运行 `drizzle-kit push`
- [x] 5.4 迁移后验证 ledger、目标 schema、主表计数、必填翻译、孤儿关系和旧列移除
- [ ] 5.5 经单独授权后部署新 schema 对应代码
- [x] 5.6 记录备份位置、校验值、迁移结果和未运行 E2E 的验证边界；若任一步失败则停止发布并按 runbook 恢复
