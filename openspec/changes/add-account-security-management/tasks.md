## 1. 数据模型与认证配置

- [x] 1.1 新增 `login_history` 数据表、事件类型、用户关联和时间/IP/User-Agent 索引。
- [x] 1.2 生成并执行登录历史表数据库迁移，验证现有用户和会话数据不受影响。
- [x] 1.3 在 Better Auth 中关闭 OAuth 隐式账号关联，并补充关联策略测试。

## 2. 认证事件记录与查询

- [x] 2.1 实现登录历史写入服务，统一处理事件类型、provider、请求 IP、User-Agent 和清理策略。
- [x] 2.2 接入成功登录、失败登录、主动退出和退出其他设备事件记录。
- [x] 2.3 新增仅限当前用户的登录历史查询接口，默认返回最近 50 条。
- [x] 2.4 为事件分类、用户隔离、失败登录脱敏和查询权限补充测试。

## 3. 关联账号管理

- [x] 3.1 扩展认证客户端和服务端 provider 配置传递，只展示实际启用的 Google/GitHub/Apple。
- [x] 3.2 实现关联账号列表、显式 OAuth 绑定和绑定成功后的刷新。
- [x] 3.3 使用项目封装 Dialog 实现解绑确认，并处理最后一个认证方式保护。
- [x] 3.4 为关联、绑定失败、解绑和 provider 未配置场景补充测试。

## 4. 账号安全页面

- [x] 4.1 新增“关联账号”Tab 和关联账号设置组件。
- [x] 4.2 新增“登录历史”Tab、加载占位、空状态和安全字段展示。
- [x] 4.3 保持移动端 Tab 布局、统一内容间距和项目封装组件规范。
- [x] 4.4 运行账号安全全量测试、类型检查、Lint 和数据库迁移验证。
