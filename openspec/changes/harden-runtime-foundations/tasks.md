## 1. 环境变量基础

- [x] 1.1 为服务端核心变量、客户端公开变量和可选集成建立 Zod Schema 与类型化访问模块
- [x] 1.2 为 URL 格式、核心变量缺失、可选集成完整/部分配置和秘密不回显编写单元测试
- [x] 1.3 新增 `src/instrumentation.ts`，在生产启动阶段执行核心环境校验并兼容 Node 与 Worker 路径
- [x] 1.4 将 next config、数据库、认证及外部集成迁移到类型化环境入口

## 2. 服务端模块边界

- [x] 2.1 为数据库、认证、tRPC context/router/service 和服务端环境入口增加 `server-only`
- [x] 2.2 为邮件、对象存储、验证码、缓存及其他秘密访问入口增加 `server-only`
- [x] 2.3 拆分被 Client Component 使用的纯类型、枚举和 Schema，消除服务端入口的间接客户端导入
- [x] 2.4 增加服务端边界静态测试，验证非法客户端导入失败且共享契约可导入

## 3. Sitemap 解耦

- [x] 3.1 先增加数据库不可访问时构建不触发 sitemap 查询的回归测试
- [x] 3.2 将 sitemap 改为运行时数据读取和明确缓存周期，移除构建期远程查询
- [x] 3.3 实现动态查询失败时仅返回安全静态 URL 的降级并接入错误报告入口
- [x] 3.4 使用 sitemap 分片覆盖超过 1000 条的文章和页面，并增加分页完整性测试

## 4. CSP 与安全响应头

- [x] 4.1 建立集中安全头生成器，按环境和已启用集成生成静态 CSP
- [x] 4.2 增加 report-only/强制模式开关及 HSTS、内容类型、来源、权限和防嵌入响应头
- [x] 4.3 为开发/生产差异、允许来源和响应头组合编写单元测试
- [x] 4.4 扩展 Playwright 覆盖登录、Analytics、Turnstile、远程图片、PWA 和管理后台 CSP 回归

## 5. 验证与发布

- [x] 5.1 在数据库网络不可用的环境运行 `bun run build` 并确认构建成功
- [x] 5.2 运行类型检查、Lint、全量单测、相关 E2E 和 Cloudflare 构建
- [x] 5.3 更新 README 环境变量、可选集成、CSP 观察模式和 sitemap 降级说明
- [ ] 5.4 先以 report-only 部署并记录验证清单，通过后切换生产强制 CSP
